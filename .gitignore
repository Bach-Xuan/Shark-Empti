# Build/test artifacts; keep durable reports at the repository root, not in generated output.
# Artifact build/test; luu bao cao ben vung o root repository, khong trong output tao tu dong.
.next/
coverage/
test-results/
playwright-report/
blob-report/
playwright/.cache/
out/
build/
dist/

node_modules/
jspm_packages/
web_modules/

.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
.pnpm-debug.log*

# Local environment variants may contain secrets. Only the template is tracked.
# Moi bien the env co the chua secret; chi theo doi template.
.env
.env.*
!.env.example

# Historical cache only; Genkit is no longer part of the runtime.
.genkit/

.firebase/
/emulator-data/
/firebase-export-*/
firebase-debug*.log
firestore-debug*.log
pubsub-debug*.log
ui-debug*.log
*-debug.log

npm-debug.log*
yarn-debug.log*
yarn-error.log*

.DS_Store
.DS_Store?
._*
Thumbs.db
ehthumbs.db
Desktop.ini

*.pem
*.key
/secrets/
*service-account*.json
*-firebase-adminsdk-*.json

*.tsbuildinfo

.vercel

.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*.db
*.sqlite

docker-compose.override.yml

# Local verification runtimes
.tools/
